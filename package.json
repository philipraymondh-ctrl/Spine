{
  "name": "spine",
  "version": "3.0.0",
  "private": true,
  "description": "A committer, not a ranker.",
  "scripts": {
    "test": "node test/spine.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
